pluginManagement {
    repositories {
        maven("https://maven.neoforged.net/releases") // pour le plugin NeoForge
        gradlePluginPortal() // plugins Gradle classiques
        mavenCentral()
    }
}

rootProject.name = "shopandeconomy"
